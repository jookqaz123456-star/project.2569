{
  "name": "samrit-residence",
  "version": "1.0.0",
  "description": "ระบบจัดการหอพักสัมฤทธิ์ — Samrit Residence Management (frontend + REST API + SQLite)",
  "type": "commonjs",
  "main": "server/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite server/server.js",
    "dev": "node --experimental-sqlite --watch server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongodb": "^7.3.0"
  }
}
